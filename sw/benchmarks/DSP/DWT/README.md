# DWT
There are two versions of the optimized kernel. The "too_opt" one uses 2d loops to manage memory accesess, but results in a lower performance gain. So, the code sticks with the opt v1.

One can change the filter size as needed.